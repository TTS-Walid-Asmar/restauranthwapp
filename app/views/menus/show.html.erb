<p id="notice"><%= notice %></p>
  
  <%= image_tag @menu.avatar.url(:medium) %>
<p>
  <strong>Name:</strong>
  <%= @menu.name %> was created by <%= @menu.user.email %>
</p>

<p>
  <strong>Price:</strong>
  <%= @menu.price %>
</p>
<br />
<br />
<br />
<%= render 'comments/form'%>
<br />
<br />
<% @menu.comments.each do |comment| %>
  <%= comment.comment_entry %><strong>  written by:  <%= comment.user.email %></strong><br /><br />
  <%end%>
<%= link_to 'Edit', edit_menu_path(@menu) %> |
<%= link_to 'Back', menus_path %>
